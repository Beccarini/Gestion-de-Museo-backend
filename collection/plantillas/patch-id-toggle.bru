meta {
  name: Toggle plantillas plantilla toggle
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/plantillas/{{plantillaId}}/toggle
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
