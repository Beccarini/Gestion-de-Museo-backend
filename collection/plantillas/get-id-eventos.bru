meta {
  name: Get plantillas plantilla eventos
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/plantillas/{{plantillaId}}/eventos
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
