meta {
  name: Get integrantes integrante permisos
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/integrantes/{{integranteId}}/permisos
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
