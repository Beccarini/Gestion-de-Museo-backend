meta {
  name: Delete integrantes integrante
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/integrantes/{{integranteId}}
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200 or 204", function() {
    expect([200, 204]).to.include(res.getStatus());
  });
}
