meta {
  name: Get integrantes integrante registros
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/integrantes/{{integranteId}}/registros
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
