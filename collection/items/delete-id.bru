meta {
  name: Delete items item
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/items/{{itemId}}
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200 or 204", function() {
    expect([200, 204]).to.include(res.getStatus());
  });
}
