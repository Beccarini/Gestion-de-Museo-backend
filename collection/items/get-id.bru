meta {
  name: Get items item
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/items/{{itemId}}
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
