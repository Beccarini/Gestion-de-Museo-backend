meta {
  name: Delete eventos evento
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/eventos/{{eventoId}}
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200 or 204", function() {
    expect([200, 204]).to.include(res.getStatus());
  });
}
