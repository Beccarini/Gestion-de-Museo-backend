meta {
  name: Get eventos evento
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/eventos/{{eventoId}}
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
