meta {
  name: Get eventos evento registros
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/eventos/{{eventoId}}/registros
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
