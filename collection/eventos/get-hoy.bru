meta {
  name: Get eventos hoy
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/eventos/hoy
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
