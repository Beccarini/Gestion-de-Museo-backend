meta {
  name: Delete recursos recurso
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/recursos/{{recursoId}}
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200 or 204", function() {
    expect([200, 204]).to.include(res.getStatus());
  });
}
