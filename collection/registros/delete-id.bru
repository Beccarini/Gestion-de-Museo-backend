meta {
  name: Delete registros registro
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/registros/{{registroId}}
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status code is 200 or 204", function() {
    expect([200, 204]).to.include(res.getStatus());
  });
}
